<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Elegant Contact Form</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Ribeye&family=VT323&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto+Flex:opsz,wght@8..144,100..1000&family=Space+Mono:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="/css/app.css"/>

  








<body >


    <!-- Fixed Navbar -->
    <nav style="background-color: rgb(0, 0, 0);" class="navbar navbar-expand-lg  fixed-top ">
      <div class="container">
        <p  class="navbar-brand" ><span id="nn">Kcars.</span><span id="nnn">in</span></p>
      </div>
    </nav>
  
    <!-- Main Content -->
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
          <div class="form-container mt-3">
            <!-- <h2 class="form-title text-center mb-4">Get in Touch</h2> -->
  
            <div class="main-heading mt-1">
              <h1><span class="auto-type"></span> </h1>
            </div>
  
            <div class="main-heading mt-1">
              <div class="marquee-container">
      <div class="marquee">
        ⭐ We do not ask for any type of Payment. We recommend to visit & Pay once you get the best offer.
      </div>
    </div>
            </div>
  
  
  <!-- 
            <label for="category"style="margin-top:" want to : </label>
            <select name="category" id="category">
                <option value="Buy" selected>Buy</option>
                <option value="Sell">Sell</option>
            </select><br>
            <label for="dropdown">Choose a  Brand : </label>
          
            <select name="dropdown" id="dropdown">
                <option value="Maruti" selected>Maruti</option>
                <option value="Hyundai">Hyundai</option>
                <option value="Mahindra">Mahindra</option>
            </select><br>
          
            <label for="variant">Enter Variant : </label>
            <input id="variant" type="text" placeholder="Alto , i20 , Thar etc..."name="variant"><br>
          
          
            <form id="phoneForm">
              <label for="phone"placeholder="10 digits only">Phone Number:</label>
              <input type="text" id="phone" name="phone"  required>
              <button type="submit" >Submit</button>
            </form> -->
  
  
  
  
              <div class="mb-3">
                <label  for="category" class="form-label mt-2">You want to : </label>
                <select  class="form-select" name="category" id="category">
                  <option value="Buy" selected>Buy</option>
                  <option value="Sell">Sell</option>
              </select><br>
  
  
              </div>
  
              <div class="mb-3">
                <label for="email" class="form-label">Choose a brand : </label>
                <select class="form-select" name="dropdown" id="dropdown">
                  <option value="Maruti" selected>Maruti</option>
                  <option value="Hyundai">Hyundai</option>
                  <option value="Mahindra">Mahindra</option>
              </select><br>
              </div>
  
              <div class="mb-3">
                <label  style="margin-bottom: 5px;" for="variant">Enter Variant : </label>
                <input class="form-control" id="variant" type="text" placeholder="Alto , i20 , Thar etc..."name="variant"><br>
              </div>
  
              <div class="mb-3">
                <form id="phoneForm">
                  <label style="margin-bottom: 5px;" for="phone">Phone Number :</label>
                  <input class="form-control" type="text" id="phone" name="phone"placeholder="10 digits only"  required>
                  <!-- <button type="submit" >Submit</button> -->
                  <div class="d-grid gap-2 col-6 mx-auto">
                    <button style="margin-top: 35px;font-family:Share Tech Mono;font-size: 18px;"
                    class="btn btn-primary" type="submit">Send</button>
                  </div>
                </form>
              </div>
  
  
          </div>
        </div>
      </div>
    </div>
  
    <!-- Fixed Footer -->
    <footer class="bg-dark text-white text-center py-3 fixed-bottom shadow-sm">
      <div class="container">
        &copy; 2025 Kcars.in
      </div>
    </footer>
  
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.12"></script>
  
    <script>
      var typed = new Typed(".auto-type",{
        strings : ["Buy","or","Sell!","Buy at best value...","Sell at best value..."],
        typeSpeed : 100,
        backSpeed : 50,
        looped : true
      })
  
      
  
  
  document.getElementById('phoneForm').addEventListener('submit', async function (e) {
    e.preventDefault();
    const catVal = document.getElementById('category').value;
    const dropdownValue = document.getElementById('dropdown').value;
    const Variantvalue = document.getElementById('variant').value;
    const phoneNumber = document.getElementById('phone').value;
    // const {category} = req.body;
  
    // Basic front-end validation
  
    if (!/^\d{10}$/.test(phoneNumber)) {
      alert("Phone number must be exactly 10 digits.");
      return;
    }
  
    try {
      const response = await fetch('/submit', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ category:catVal, dropdown: dropdownValue,variant:Variantvalue,phone:phoneNumber })
      });
  
      const result = await response.json();
      alert(result.message);
      
    } catch (error) {
      alert("Submission failed: " + error.message);
    }
    
    var allInputs = document.querySelectorAll('input');
    allInputs.forEach(singleInput => singleInput.value = '')
  
  });
  
    </script>
  </body>